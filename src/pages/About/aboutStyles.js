import styled from "styled-components";
import { motion } from "framer-motion";

export const AboutStyles = styled(motion.section)`
  width: 100%;
  height: 100vh;
  display: grid;
  place-items: center;
`;
